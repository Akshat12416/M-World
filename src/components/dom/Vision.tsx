export default function Vision() {
  return (
    <section id="vision" className="section bg-charcoal" style={{ minHeight: '150vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <div className="container" style={{ textAlign: 'center' }}>
        <h2 style={{ fontSize: 'clamp(3rem, 7vw, 6rem)', lineHeight: 1.1, letterSpacing: '-0.02em', maxWidth: '1000px', margin: '0 auto' }} className="vision-text">
          Just as humans have companies, AI will have companies.
        </h2>
      </div>
    </section>
  );
}
